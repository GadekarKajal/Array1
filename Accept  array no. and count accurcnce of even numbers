#include<stdio.h>
int main()
{
   int a[100],i,num,c=0;
   printf("enter limit");
   scanf("%d",&n);
   printf("enter number");
   for(i=0;i<n;o++)
   scanf("%d",&a[i]);
   for(i=0;i<n;i++)
    {
       if(a[i]=num)
        {
           c++
        }
  printf("\n accurcnce of number=%d",c);
   }
}
